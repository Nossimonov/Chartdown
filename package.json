{
  "name": "chartdown-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Chartdown reference implementation workspace ??? see ADR 0007",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node build.mjs && tsc -p packages/core/tsconfig.build.json && tsc -p packages/render-svg/tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "bump": "node bump.mjs",
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/render-svg && tsc --noEmit -p packages/cli && tsc --noEmit -p packages/browser && tsc --noEmit -p packages/obsidian && tsc --noEmit -p packages/mcp && tsc --noEmit -p packages/measure && tsc --noEmit -p packages/action && tsc --noEmit -p playground",
    "preflight:plugin": "node plugin-preflight.mjs"
  },
  "devDependencies": {
    "@rgrove/parse-xml": "^5.0.0",
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "happy-dom": "^20.12.0",
    "typescript": "^7.0.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=20"
  }
}
